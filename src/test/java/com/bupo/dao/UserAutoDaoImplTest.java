package com.bupo.dao;

import org.junit.Test;

public class UserAutoDaoImplTest {
	@Test
	public void saveUser() {
//		UserAutoDaoImpl autoDaoImpl = new UserAutoDaoImpl();
//
//		// Write
//		String emailId = "sudhamsh.b@g.com";
//		UserAuto user = new UserAuto(emailId, "S", "Bachu");
//		autoDaoImpl.save(user);
//
//		// Read
//		user = (UserAuto) autoDaoImpl.findById(emailId);
//		Assert.assertEquals("Couldn't find the user who was created just now", emailId, user.getEmail());
//
//		// Read random user
//		user = (UserAuto) autoDaoImpl.findById(System.currentTimeMillis() + "@g.com");
//		System.out.println("user ::: " + user);
//		Assert.assertNull("Found user who shouldn't exist", user);
//
//		CassRepository.closeConnection();

	}
}
